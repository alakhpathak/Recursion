namespace Learnings
{
    using System;
    class Program
    {
        public static void Main(string[] args)
        {
            ///Console.WriteLine(AllSteps(4, new int[] { 1, 2, 3 }, 0, 4, new List<int>()));
            ///Console.WriteLine(AllKLength("abcd", string.Empty, 3, new int[4] { 1, 1, 1, 1 }));
            ///var hashSet = new HashSet<string>();
            ////AllValidParenthesis(3, 3, 3, 0, hashSet, new char[6]);
            ///var x = AllValidParenthesis2(2, 2, 2, hashSet, string.Empty);
            ///Console.WriteLine(string.Join(",", hashSet));///
        }

        
       

   

       
    }
}
